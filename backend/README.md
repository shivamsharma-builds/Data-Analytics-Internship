# HR Attrition API

FastAPI backend for the HR Attrition Analytics dashboard.

```powershell
python -m pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 8000
```

Swagger: `http://localhost:8000/docs`

Run tests:

```powershell
python -m pytest -q
```
